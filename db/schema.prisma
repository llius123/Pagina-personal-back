generator client {
  provider = "prisma-client-js"
}

datasource sqlite {
  provider = "sqlite"
  url      = "file:./db.db"
}

model USER {
  ID       Int    @default(autoincrement()) @id
  USERNAME String
}
